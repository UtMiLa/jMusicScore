{
  "name": "noteeditor",
  "version": "1.0.0",
  "description": "demo of note editor in browser",
  "main": "app1.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "npm install ../japps/ && npm install ../jmusicscore/ && npm install pegjs && npm install gulp-pegjs"
  },
  "dependencies": {},
  "author": "John Ekberg",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1"
  }
}
